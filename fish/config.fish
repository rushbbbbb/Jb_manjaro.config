if status is-interactive
    # Commands to run in interactive sessions can go here
end
alias _pcs='sudo proxychains'
alias _lyf='proxychains sudo pacman -S '
# alias _telegram='/home/a/snap/_telegram'
# alias _bjz='sudo pacman -S '
alias _start_docker='sudo systemctl start docker.service'
alias _uc='bash /home/a/.config/clash/update_subscription.sh'
alias _up='sudo pacman -Syu'
alias _clr='clear'